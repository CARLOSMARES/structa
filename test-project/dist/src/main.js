// Generated by Structa v0.7.0

import { server } from '../runtime.js';

export class UserController {
    async findAll(req, res) {
        return  {id: "1",name: "Juan",email: "juan@test.com"}, {id: "2",name: "Maria",email: "maria@test.com"}
    }
    async findById(req, res) {
        return {id: req.params.id,name: "Usuario "+req.params.id,email: "user"+req.params.id+"@test.com"}
    }
    async create(req, res) {
        return {id: Date.now().toString(),...req.body}
    }
    async update(req, res) {
        return {id: req.params.id,...req.body}
    }
}
export class HomeController {
    async index() {
        return { name: "Structa API", version: "0.7.0", status: "running" }
    }
    async health() {
        return {status: "ok"}
    }
}

// Routes
server.route({ method: 'GET', path: '/api/users/', handler: async (req, res) => { const ctrl = new UserController(); return await ctrl.findAll(req, res); } });
server.route({ method: 'GET', path: '/api/users/:id', handler: async (req, res) => { const ctrl = new UserController(); return await ctrl.findById(req, res); } });
server.route({ method: 'POST', path: '/api/users/', handler: async (req, res) => { const ctrl = new UserController(); return await ctrl.create(req, res); } });
server.route({ method: 'PUT', path: '/api/users/:id', handler: async (req, res) => { const ctrl = new UserController(); return await ctrl.update(req, res); } });
server.route({ method: 'GET', path: '/', handler: async (req, res) => { const ctrl = new HomeController(); return await ctrl.index(req, res); } });
server.route({ method: 'GET', path: '/health', handler: async (req, res) => { const ctrl = new HomeController(); return await ctrl.health(req, res); } });

server.listen();
